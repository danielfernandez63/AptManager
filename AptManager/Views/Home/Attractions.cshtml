@{
    ViewBag.Title = "Attractions In Your Area!";
}
<h2>@ViewBag.Title</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <text>
            var marker = new google.maps.Marker({
            map: map,
            position: { lat: @item.item1, lng: @item. },
            title: 'Hello World!'
            });
        </text>
    }

</table>